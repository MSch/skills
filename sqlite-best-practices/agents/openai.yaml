interface:
  display_name: "SQLite Best Practices"
  short_description: "SQLite schema and migration patterns"
  default_prompt: "Review this SQLite-backed app and improve its schema, connection setup, and migration flow using the SQLite best-practices pattern."
